:root {
    --bg: #f0f2f5;
    --sidebar: #1e272e;
    --accent: #00d2d3;
    --card: #ffffff;
    --danger: #ff7675;
}

body { font-family: sans-serif; margin: 0; background: var(--bg); overflow: hidden; }

/* LAYOUT */
.app-container { display: flex; height: 100vh; }
.sidebar { width: 70px; background: var(--sidebar); display: flex; flex-direction: column; align-items: center; padding: 20px 0; }
.content { flex: 1; display: flex; flex-direction: column; overflow-y: auto; }

/* KANBAN */
.kanban-board { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; padding: 20px; flex: 1; }
.kanban-column { background: #dfe6e9; border-radius: 12px; padding: 15px; min-height: 400px; }
.card { background: white; padding: 15px; margin-bottom: 12px; border-radius: 8px; cursor: grab; box-shadow: 0 2px 5px rgba(0,0,0,0.1); border-left: 5px solid var(--accent); }

/* MODALS */
.modal-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.7); display: none; justify-content: center; align-items: center; z-index: 2000; }
.modal-content { background: white; padding: 30px; border-radius: 15px; max-width: 450px; width: 90%; }

/* VISUAL MODE OVERRIDE */
body.visual-mode .card { height: 120px; border-bottom: 3px solid #ccc; background-image: url('https://www.transparenttextures.com/patterns/graphy.png'); }

/* BUTTONS */
.btn-confirm { background: var(--accent); color: white; border: none; padding: 10px 20px; border-radius: 5px; cursor: pointer; }
.btn-danger { background: var(--danger); color: white; border: none; padding: 8px 15px; border-radius: 5px; cursor: pointer; }

/* SIDEBARS */
.details-sidebar { position: fixed; right: -400px; top: 0; width: 350px; height: 100%; background: white; box-shadow: -5px 0 15px rgba(0,0,0,0.2); transition: 0.3s; padding: 20px; z-index: 1500; }
.details-sidebar.open { right: 0; }
.tab-content textarea { width: 100%; height: 200px; margin-top: 10px; }
